dist
node_modules

# Generated by `pnpm gen:tokens`. The formatting is the generator's output;
# reformatting it here would be reverted on the next run and then show up as a
# dirty diff in .claude/suite.json's `generated` check.
src/styles/tokens.css

# Same reasoning, different generator: `pnpm gen:commands` owns the layout of
# everything under here, and it carries a DO-NOT-EDIT header.
src/data/generated

# pnpm owns the lockfile's layout and rewrites it on any dependency change, so a
# Prettier pass over it is undone the next time anyone adds a package.
pnpm-lock.yaml

# Vendored from kollektiv/design/tokens.json by that repo's scripts/sync-tokens.sh,
# which byte-compares the two to detect drift. Prettier reformatting it here would
# report drift that does not exist — the same reason .gitattributes pins LF.
tokens.source.json
