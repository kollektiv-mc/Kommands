dist
node_modules

# Generated by `pnpm gen:tokens`. The formatting is the generator's output;
# reformatting it here would be reverted on the next run and then show up as a
# dirty diff in .claude/suite.json's `generated` check.
src/styles/tokens.css

# Same reasoning, different generator: `pnpm gen:commands` owns the layout of
# everything under here, and it carries a DO-NOT-EDIT header.
src/data/generated

# pnpm owns the lockfile's layout and rewrites it on any dependency change, so a
# Prettier pass over it is undone the next time anyone adds a package.
pnpm-lock.yaml

# Vendored from kollektiv/design/tokens.json by that repo's scripts/sync-tokens.sh,
# which byte-compares the two to detect drift. Prettier reformatting it here would
# report drift that does not exist — the same reason .gitattributes pins LF.
tokens.source.json

# Vendored from kollektiv/plugins/suite-kit/ by that repo's scripts/sync-priority.sh,
# which byte-compares the two to detect drift. Same reasoning as tokens.source.json
# above: a Prettier pass here would be reverted on the next sync and would show up
# in between as drift that does not exist.
.github/workflows/issue-priority.yml

# Vendored from kollektiv/plugins/suite-kit/workflows/ by that repo's
# scripts/sync-workflows.sh, which byte-compares the two to detect drift. Same
# reasoning as issue-priority.yml above.
.github/workflows/aislop.yml
.github/workflows/codeql.yml
.github/workflows/pr-labelled.yml
.github/workflows/scorecard.yml

# Vendored from kollektiv/.aislop/base.yml by that repo's scripts/sync-aislop.sh, which
# byte-compares the two. Same reasoning as the entries above.
.aislop/base.yml

# Vendored by kollektiv's scripts/sync-notes.sh beside the release-notes generator.
.github/release.yml

# Wails' own packaging templates, copied verbatim from the CLI's buildassets
# and expanded by `wails build` from wails.json. info.json is the only one
# Prettier can parse, and reformatting it would put this repo's layout on a
# file upstream owns and regenerates, for no gain.
build/windows/
